#include "main.h"

/**
* mul - adds two integers and returns the result.
* @a: frist factor.
* @b: second factor.
* Return: 0 if exited correctly, non-zero otherwise.
*/

int mul(int a, int b)
{
	return (a * b);
}
