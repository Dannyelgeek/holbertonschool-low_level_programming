#include <stdio.h>

/**
* main -  prints the alphabet in lowercase and uppercase.
* Return: 0 if exited correctly, non-zero otherwise.
*/

int main(void)
{
	int ch, CH;

	for (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);

	for (CH = 'A'; CH <= 'Z'; CH++)
	putchar(CH)

	putchar('\n');
	return (0);
}
