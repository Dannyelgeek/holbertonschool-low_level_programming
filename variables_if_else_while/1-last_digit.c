#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - print the last digit of the number stored in the variable n.
* Return: 0 if exited promerly, non-zero otherwise.
*/

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
return (0);
}
